# device_uuid

A plugin to get device's UUID

## Getting Started

Add this dependency to `pubspec.yaml` file
```yaml
  device_uuid: ^0.0.4
```

## Using
```dart
final uuid = DeviceUuid().getUUID();
```
